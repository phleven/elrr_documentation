Change Eclipse Formatting Styles to use spaces instead of Tabs
Change Eclipse Formatting Styles to 80 characters per line
Change XML Editor Formatting
Open .pom


1. Eclipse --> Window --> Preferences --> Java --> Code Style --> Formatter
   a. Edit
      1. Profile name = Eclipse ELRR
      2. Indentation --> Tab policy = Spaces only
      3. Line Wrapping --> Maximum line width = 80
      4. Comments --> Maximum width for comments = 80
      5. Method declarations --> declaration = wrap where necessary
      6. Wrapping Settings --> Annotations --> On Parameters
         a. Wrap all elements, every element on a new line.
      7. Wrapping Settings --> Annotations --> Type Annotations
         a. Wrap all elements, every element on a new line.
      8. Wrapping Settings --> Annotations --> Element value pairs
         a. Wrap all elements, except first element if not necessary.
      9. Wrapping Settings --> Method Declarations --> Declarations
         a. Do not wrap.
     10. Wrapping Settings --> Method Declarations --> Parameters
         a. Wrap where necessary.
     11. Wrapping Settings --> Method Declarations --> throws clause
         a. Wrap where necessary.
     12. Comments --> JavaDocs
         a. Align javadoc tags in columns = Don't align
     13. Line Wrapping
         a. Never join already wrapped lines
          


      
1. Eclipse --> Window --> Preferences --> XML --> XML files --> Editor
   a. Check indent using tabs
   b. Indentation size = 1 



Open .pom  
1. Right click on .pom file --> Open with --> XML Editor